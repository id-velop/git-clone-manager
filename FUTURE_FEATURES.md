# 后续功能规划

本文档记录 Quick Clone 后续可能加入的功能方向，作为产品讨论和开发排期的参考。具体交互、视觉方案和优先级可在实现前进一步确认。

## 1. 标题 + 说明

### 功能描述

在页面标题旁增加“+”入口，并配合简短说明，让用户更容易理解当前页面的用途，也为后续扩展操作预留明确位置。

### 交互建议

- “+”应与标题保持稳定的视觉关系，在不同窗口尺寸下都容易发现。
- 点击“+”后，可根据页面上下文打开新增、添加仓库或其他快捷操作面板。
- 说明文案应简短直接，优先解释用户能在当前页面完成什么。
- 如果当前没有可用操作，应隐藏入口或置灰，并提供合理的提示。

### 待确认事项

- “+”对应的首个具体操作是什么。
- 入口是否需要支持快捷键或悬浮提示。
- 标题和说明在扩展弹窗较窄时的折行方式。

## 2. 头像增加 Git 相关图标和情感化元素

### 功能描述

在头像中加入 Git 生态相关图标，或使用猫等轻量、友好的情感化形象，让不同代码托管来源和使用状态更直观，也增强产品识别度。

### 视觉方向

- 使用 GitHub、GitLab、分支、提交等相关符号作为头像角标或辅助元素。
- 可提供猫等开发者熟悉的情感化形象，保持整体风格轻松但不过度干扰信息识别。
- Git 平台或仓库来源可以通过图标、角标或头像样式区分。
- 状态变化可以使用有限的表情或形象变化表达，例如成功、进行中和失败。

### 体验要求

- 图标和情感化元素不能替代仓库名称、平台名称等关键信息。
- 头像需要保留清晰的默认兜底样式，避免加载失败时影响使用。
- 图标尺寸较小时仍应可辨识，并满足基本的对比度和可访问性要求。
- 不同平台的品牌图标使用时应遵守对应的品牌规范。

### 待确认事项

- 首期支持哪些 Git 平台和状态。
- 使用静态头像、状态角标，还是根据状态动态切换形象。
- 情感化元素的数量、插画风格和是否需要用户自定义。

## 建议的后续步骤

1. 确认两个方向的首期目标和使用场景。
2. 为标题“+”入口和头像方案各制作一版低保真交互稿。
3. 在扩展弹窗中验证尺寸、可读性和状态反馈。
4. 确认视觉资源及品牌规范后，再拆分开发任务。
